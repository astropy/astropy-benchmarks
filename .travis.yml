python:
  - 2.7
  - 3.3

matrix:
  - python: 2.7

install:
  - sudo pip install git+git://github.com/spacetelescope/asv.git@master
  - cp machine.json $HOME/.asv-machine.json
  - export PATH=/opt/pyenv/versions/3.3.5/bin/:$PATH

script:
  - asv run -q --machine-defaults master
